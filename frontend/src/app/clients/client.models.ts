export interface ClientModelForm{
  clientId?: string;
  name: string;
  email: string;
  phone: string;
}

export interface ClientModelTable{
  clientId: string;
  name: string;
  email: string;
  phone: string;
}
